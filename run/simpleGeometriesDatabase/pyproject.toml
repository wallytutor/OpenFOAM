[project]
name = "simplegeometriesdatabase"
version = "0.1.0"
description = "Create simple geometries for testing purposes."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gmsh>=4.15.2",
    "ipython>=9.16.0",
]
